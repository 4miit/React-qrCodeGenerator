import React from 'react'
import ReactDOM from 'react-dom/client'
import QrGenerate from './App.tsx'


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QrGenerate />
  </React.StrictMode>,
)
